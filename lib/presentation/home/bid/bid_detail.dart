import 'package:flutter/material.dart';

class BidDetails extends StatelessWidget {
  const BidDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Bid Details'),);
  }
}
