class ChatArgs {
  String name;
  String image;
  String uid;

  ChatArgs(this.name, this.image, this.uid);
}

class BidArgs {
  String? image, docId, uid;

  BidArgs(this.image, this.docId, this.uid);
}
